# Test properties to override dev database configuration during tests
# Since tests run on H2 database, we disable Flyway and let Hibernate create the schema.

spring.flyway.enabled=false
spring.jpa.hibernate.ddl-auto=create-drop
